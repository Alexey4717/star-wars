import { RocketOutlined } from '@ant-design/icons';

function App() {
  return (
    <main
      style={{
        minHeight: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 24,
      }}
    >
      <RocketOutlined />
      star wars app
    </main>
  );
}

export default App;
